/** Default grid font stack — keep in sync with `--lfg-font-family` on `.lfg-grid`. */
export const DEFAULT_GRID_FONT_FAMILY =
  'system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif';

/** Default grid base font size — keep in sync with `--lfg-font-size` on `.lfg-grid`. */
export const DEFAULT_GRID_FONT_SIZE = "14px";
