import type { FloatingFiltersOptions } from "@lightfastgrid/core";

/** Global floating-filter row for the demo. */
export const gridDemoFloatingFilters: FloatingFiltersOptions = {
  enabled: true,
  debounceMs: 250,
  menuButton: false,
};
