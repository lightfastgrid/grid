import type { LightFastGridDefaultColDef } from "@lightfastgrid/core";

/** Production-style defaults for declarative columns. */
export const gridDemoDefaultColDef: LightFastGridDefaultColDef = {
  sortable: true,
  filter: true,
  resizable: true,
  reorderable: true,
  floatingFilter: true,
};
